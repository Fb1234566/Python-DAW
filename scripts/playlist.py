import tkinter as tk

def create_playlist(win,tracks=[]):
    canvas=tk.Canvas(win, height=500, width=500)
    def cerate_track():
        label
    canvas.pack()

create_playlist(tk.Tk())
tk.mainloop()